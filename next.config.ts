import type { NextConfig } from "next";

const nextConfig = {
    images: {
      domains: ["www.unitedautopart.com","static.beebom.com", ], // <-- add this line
      
    },
  };

export default nextConfig;
